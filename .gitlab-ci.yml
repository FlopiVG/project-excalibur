

stages:
  - test
  - deploy

before_script:
  - yarn install

cache:
  paths:
    - node_modules/

test:lint:
  image: node:9.11.1
  script:
    - yarn lint

#test:unit-test:
#  script:
#    - yarn test

deploy_staging:
  image: ruby:2.3
  stage: deploy
  script:
    - gem install dpl
    - dpl --provider=heroku --app=proyect-excalibur --api-key=$HEROKU_API_KEY --strategy=git
  environment:
    name: heroku
    url: https://proyect-excalibur.herokuapp.com
  only:
    - develop
